输入客户访问的网址，工具会自动检查 DNS 解析、TCP 连通性、TLS 信任、证书链、域名匹配和有效期，并生成结构化诊断结果，帮助支持人员快速判断问题所在。

核心能力

检查公共 DNS 与本机 DNS 解析情况
验证 443 端口连通性与 TLS 握手状态
读取并分析服务器证书与证书链
判断证书是否过期、未生效或域名不匹配
辅助识别缺少中间证书、自签名证书、代理或安全软件替换证书等问题
一键生成可复制报告，便于沟通与留档
